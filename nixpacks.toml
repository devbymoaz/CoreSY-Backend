[phases.setup]
  nixPkgs = ["..."]

[phases.install]
  cmds = ["npm install"]
  dependsOn = ["setup"]

[phases.build]
  cmds = ["npm run prisma:generate", "npm run prisma:deploy"]
  dependsOn = ["install"]

[phases.start]
  cmd = "npm start"
  dependsOn = ["build"]
